<UserControl x:Class="MojaBiblioteka.MVP.BookCard.View.BookCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MojaBiblioteka.MVP.BookCard.View"
             xmlns:converters="clr-namespace:MojaBiblioteka.Utility.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:ByteArrayToImageSourceConverter x:Key="ByteArrayToImageSourceConverter"/>
        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.12" BlurRadius="12" ShadowDepth="2" Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Style="{StaticResource CardBorder}">
        <Border.Triggers>
            <EventTrigger RoutedEvent="MouseEnter">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                         To="1.03" Duration="0:0:0.15"/>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                         To="1.03" Duration="0:0:0.15"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
            <EventTrigger RoutedEvent="MouseLeave">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                         To="1.0" Duration="0:0:0.15"/>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                         To="1.0" Duration="0:0:0.15"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>

        <Border.RenderTransform>
            <ScaleTransform ScaleX="1" ScaleY="1"/>
        </Border.RenderTransform>

        <Border.RenderTransformOrigin>0.5,0.5</Border.RenderTransformOrigin>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Okładka -->
            <Border Grid.Row="0" CornerRadius="8,8,0,0" ClipToBounds="True">
                <Image x:Name="BookCoverImage"
                       Source="{Binding CoverImageData, Converter={StaticResource ByteArrayToImageSourceConverter}}"
                       Stretch="UniformToFill">
                    <Image.Style>
                        <Style TargetType="Image">
                            <Style.Triggers>
                            <Trigger Property="Source" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Image.Style>
                </Image>
            </Border>

            <!-- Placeholder gdy brak okładki -->
            <Border Grid.Row="0" CornerRadius="8,8,0,0">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding CoverImageData}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Border Background="#E8EAF6">
                    <TextBlock Text="Brak okladki" FontSize="16"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Border>
            </Border>

            <!-- Tytuł i Autor -->
            <Border Grid.Row="1" Padding="10,8" Background="#FFFFFF"
                    CornerRadius="0,0,8,8">
                <StackPanel>
                    <TextBlock x:Name="TitleText"
                               Text="{Binding Title}"
                               FontSize="13"
                               FontWeight="SemiBold"
                               Foreground="#1A1A2E"
                               TextTrimming="CharacterEllipsis"
                               MaxWidth="140"
                               ToolTip="{Binding Title}"/>
                    <TextBlock x:Name="AuthorText"
                               Text="{Binding Author}"
                               FontSize="11"
                               Foreground="#6B7280"
                               TextTrimming="CharacterEllipsis"
                               MaxWidth="140"
                               Margin="0,2,0,0"
                               ToolTip="{Binding Author}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
