<Window x:Class="MojaBiblioteka.MVP.MainView.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Moje książki" Height="750" Width="1200" WindowStartupLocation="CenterScreen" MinWidth="1200" MinHeight="750"
        Loaded="MainView_OnLoaded">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="1.5*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="1.5*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Row="0"  Margin="10,10,100,10" CornerRadius="30,1,30,1" Background ="Purple">
            <TextBlock Name="txtWelcome" Text="Witaj" HorizontalAlignment="Center" VerticalAlignment="Center" 
               Foreground="White" FontSize="24" FontWeight="Bold" Padding="0,10"/>
        </Border>

        <TextBox Grid.Row="0" Grid.Column="1" Name="txtSearch"
                 FontSize="18" Height="40" Margin="10,15,10,20"
                 VerticalContentAlignment="Center" HorizontalAlignment="Stretch"
                 TextChanged="txtSearch_TextChanged"/>

        <StackPanel Grid.Column ="0" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,0" Orientation="Horizontal" Grid.Row="1" Grid.ColumnSpan="2">
            <Button Name="btnDodaj" Content="Dodaj" Width="120" Height="40" Margin="5" Background="DarkGreen" Foreground="White" FontSize="20" Click="btnDodaj_Click"/>
            <Button Name="btnEdytuj" Content="Edytuj" Width="120" Height="40" Margin="5" Background="Orange" Foreground="White" FontSize="20" Click="btnEdytuj_Click"/>
            <Button Name="btnUsun" Content="Usuń" Width="120" Height="40" Margin="5" Background="Red" Foreground="White" FontSize="20" Click="btnUsun_Click"/>
        </StackPanel>

        <ListBox Grid.Row="2"
                 Grid.ColumnSpan="2"
                 Name="lvBooks"
                 BorderThickness="0"
                 Background="Transparent"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 SelectionMode="Single"
                 MouseDoubleClick="lvBooks_MouseDoubleClick">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel IsItemsHost="True"
                               Orientation="Horizontal"
                               ItemWidth="180"
                               ItemHeight="280"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Margin" Value="0,0,14,14"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
    </Grid>
</Window>
